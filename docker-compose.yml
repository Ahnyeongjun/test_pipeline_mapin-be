version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: mapin-postgres
    restart: always
    environment:
      POSTGRES_DB: mapin
      POSTGRES_USER: mapin
      POSTGRES_PASSWORD: mapin
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  qdrant:
    image: qdrant/qdrant:latest
    container_name: mapin-qdrant
    restart: always
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  postgres_data:
  qdrant_data:
